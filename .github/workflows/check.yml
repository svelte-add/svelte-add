name: Check every commit and pull request

on:
  pull_request:
  push:

jobs:
  check:
    strategy:
      matrix:
        node-version: [14.16.1, 16.1.0]
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
          registry-url: https://registry.npmjs.org/
      - uses: pnpm/action-setup@v2.0.1
        with:
          version: 6.2.x
      - run: pnpm install
      - run: pnpm check
